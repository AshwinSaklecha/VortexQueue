# Root-level env — only needed when running via Docker Compose.
# Copy to .env and fill in your values:
#   cp .env.example .env
#
# POSTGRES_PASSWORD is used by the postgres container AND injected into
# DATABASE_URL for the api/worker/janitor containers.
POSTGRES_PASSWORD=yourpassword
